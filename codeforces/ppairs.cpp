#include <bits/stdc++.h>

using namespace std;

int dp[2005];
string inp;

// main code begins now

int main()
{
    cin >> inp;
    for (int i = 0; i < ; ++i) {
    	for (int i = 0; i < ; ++i) {
    		
    	}
    }
    return 0;
}