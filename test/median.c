#include <stdio.h>


int task(int *a,int start,int end)
{
	int pivot
}


int main()
{
	int a[1000];
	int i,j,k,temp,num;
	printf("enter the number of elements:\n");
	scanf("%d",&num);
	for (i = 0; i < num; ++i)
	{
		scanf("%d",a[i]);
	}
	
	task(a,0,num-1);
}