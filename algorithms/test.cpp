#include <bits/stdc++.h>

using namespace std;




// main code begins now

int main()
{
    int n;
    string input;
    getline(cin,input);
    vector<list<int> > nodes[100005];
    for (int i = 0; i < ; ++i) {
    	
    }
    return 0;
}