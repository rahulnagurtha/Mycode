#include <bits/stdc++.h>

using namespace std;


// main code begins now

int main()
{
    int n,s;
    vector<deque<int> >current[2];
    scanf("%d%d",&n,&s);
    for (int i = n-1; i > 1; --i) {
    	
    }

    return 0;
}